# ===========================================
# runner-python (sandbox test runner) — Environment Example
# ===========================================
# Copy to `.env` if you need to override defaults.

# Per-test execution timeout in seconds (default: 2.0).
TEST_TIMEOUT_S=2.0
