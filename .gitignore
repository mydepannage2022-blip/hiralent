node_modules/
.env

# Wave 0 / Session 1 — secure snapshot of live .env files (never commit; holds creds)
.env-snapshots/

/src/generated/prisma
/config/firebase-adminsdk.json

dist
backend/uploads/
*.zip
node_modules_old/

.venv/
assessment-ai-service/venv/

# Chroma local DB
ai-service/chroma_db/
*.sqlite3
ai-service/venv/

# Python cache files
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# VSCode settings

# Wave 0 / Session 6 — committed runtime & junk artifacts (never commit; keep local)
*.log
*.lnk
# NOTE: intentionally NOT `*.db` (would collide with other tracked .db). Ignore specific ones.
dev.db
ai-service/app/crawler/scraped_data.db
backups-files-folders/
test-signup.json
temp_processing_*.pdf
ai-service/test_diagram_output.png

# Wave 1 / Session 6 — throwaway venv the internal-service-auth verifier bootstraps
hiralent-master-plan/tools/.venv-authtest/
