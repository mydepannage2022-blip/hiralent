"use client";
import React from 'react';

// Legacy editor replaced by CodeRunner. Keep a minimal no-op placeholder
// so imports (if any) don't break builds during cleanup. This file can
// be fully removed later if desired.

export default function CodeEditor() {
  return null;
}
