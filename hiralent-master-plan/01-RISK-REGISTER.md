# 01 — Risk Register

> Every material risk found in the audit, ranked. Each has an ID (stable, referenced from wave files), severity, the pillar it threatens, and the **wave that closes it**. Severity: 🔴 Critical · 🟠 High · 🟡 Medium · ⚪ Low.
> "Fixed in" points to the wave where the fix lands; verification is part of that wave's exit criteria.

## 🔴 Critical

| ID | Risk | Impact | Fixed in |
|---|---|---|---|
| R-01 | `JWT_SECRET=yourSuperSecretKey` | Anyone can forge tokens → full auth/authz bypass, superadmin takeover | Wave 1 |
| R-02 | Live Gemini API key committed in `ai-service/app/gemini_service.py:47` | Key theft, billing abuse, must be treated as compromised | Wave 1 |
| R-03 | Code-execution can run on the host (sandbox stubs + dispatcher host fallback + unauth runner HTTP stub) | Remote code execution on backend host | Wave 1 (lock path) + Wave 4 (real sandbox) |
| R-04 | Frontend `next build` fails; backend 153 TS errors | Cannot produce a production build at all | Wave 0 |
| R-05 | Payments entirely fake (no Stripe/PayPal SDK) | No revenue possible; "paid" users unpaid | Wave 5 |
| R-06 | 88 `new PrismaClient()` → DB connection exhaustion | Backend collapses in low-hundreds of concurrent users | Wave 2 — **S1 sweep ✅** (all 83 `src/**` files on one singleton; `verify-prisma-singleton.mjs`) · **S2 pooling ✅** (bounded `connection_limit=10`/`pool_timeout`; `directUrl` for migrations; PgBouncer service defined; live-proven by `verify-connection-pool.mjs` — pool caps at the limit under load) · **S7 gate ✅** (composed `verify-e2e-fullpath.mjs` — server pool bounded ≤ `connection_limit` under 30 concurrent reads; `verify-guard-teeth.mjs` proves reintroducing a stray `new PrismaClient()`/`$disconnect()` flips the gate red) · **hostile-review hardening (2026-07-29)**: the pool bound was only proven against `.env.example` — added `assertDbPoolConfig()` (runtime `DATABASE_URL` inspected at boot, **fatal in prod** if `connection_limit` missing; wired in `server.ts`, gated by `verify-db-pool-config.mjs`); the singleton also no longer `process.exit(1)`s at import time (F2 — `connectWithRetry` + graceful shutdown); singleton gate got an obfuscation guard so a client hidden in a template literal can't slip past (F5) · PgBouncer **live wiring** + per-worker sizing → **Wave 8** |
| R-07 | `SHADOW_DATABASE_URL=${DATABASE_URL}` (latent — `schema.prisma` never declared `shadowDatabaseUrl` & `dotenv@16` doesn't expand `${...}`, so the var was inert) | Any `prisma migrate dev` wipes the primary/production DB **if** shadow ever gets wired to primary | ✅ **Wave 0 / Session 5 + Wave 2 / Session 3** — line commented + documented; schema kept free of `shadowDatabaseUrl`; guarded by `verify-config-hygiene.mjs`; separation re-asserted by `verify-migration-safety.mjs`, and the fresh-deploy chain is proven on a self-created **throwaway** DB (never the primary); re-exercised end-to-end by `verify-e2e-fullpath.mjs` (fresh `migrate deploy` in the composed journey, S7) |

## 🟠 High

| ID | Risk | Impact | Fixed in |
|---|---|---|---|
| R-08 | Unauthenticated doc-validation webhook | KYC/verification spoofing | Wave 1 |
| R-09 | Unauthenticated `POST /api/ocr` (no auth/size/type) | Resource-abuse DoS, arbitrary uploads | Wave 1 |
| R-10 | IDOR on `/submissions/:id` + `/stream/:id`; `POST /submissions` accepts body `userId` | Read any candidate's code; create submissions as anyone | Wave 1 |
| R-11 | CVs (PII) served static from `/uploads`, no auth | GDPR / data exposure | Wave 1 |
| R-12 | No Dockerfile for backend API or frontend; no full-stack orchestration | Cannot deploy the two core services | Wave 8 |
| R-13 | `host.docker.internal` + hardcoded `localhost` in backend source | Inter-service calls & email links break on staging/prod | 🔶 **env-drive half ✅ Wave 3 / Session 6 (2026-08-04)** — `config/appUrls.ts` (`getFrontendUrl`/`getBackendUrl`, lazy) centralises the scattered `FRONTEND_URL` defaults across 18 sites + fixes the doc-validator `BACKEND_URL` :4000→:5000; all 15 `question.controller` AI-service fetches env-driven (`AI_SERVICE_URL` fallback); MinIO/`host.docker.internal` already clean in BE source. Proven by `verify-api-config.mjs` (BE stub-server) + re-exercised by `verify-wave3-e2e.mjs`. **Container/host-network wiring → Wave 8.** |
| R-14 | Secrets baked into Docker images (`.dockerignore` gaps) | Credential leak in shipped images | Wave 8 (+ rotate in Wave 1) |
| R-15 | No graceful shutdown / crash handlers; stuck-`RUNNING` submissions | Lost/duplicated work, zombie states on every deploy | Wave 7 |
| R-16 | In-memory queues + doc-validator job dict | Job/result loss on restart | Wave 7 |
| R-17 | BullMQ `attempts:1, removeOnFail:true` (no DLQ) | Silent job loss on transient failure | Wave 7 |
| R-18 | Python AI services block event loop; single worker; no retries/circuit-breakers on AI calls | AI features serialize; slow AI cascades to full outage | Wave 6 |
| R-19 | Not horizontally scalable (Socket.IO no Redis adapter, in-proc SSE, cron per instance, MemoryStore limiter, local uploads) | Adding instances breaks realtime, dupes work, splits limits | Wave 6 |
| R-20 | No caching + ~153 unpaginated `findMany` | Every read hits DB; unbounded payloads; DB overload | **Wave 2 / Session 5** (pagination half) — shared `parsePagination` clamp + 7 busiest list endpoints capped/bounded (hostile `?limit=99999` → ≤ cap, live-proven) + count-misuse → DB `groupBy`; non-breaking (headers, bodies unchanged). Aggregation reads (`getClientsForAgency`, dashboard analytics) flagged for DB-side aggregation follow-up. **Caching → Wave 6.** |
| R-21 | No superadmin/role-permission seed; no migration step in deploy | Fresh DB unusable (no admin, unmigrated) | ✅ **Wave 2 / Session 3** (seed half) — idempotent prod-safe superadmin + RolePermission (4×12) + plans/badges seed; `db:migrate` = `prisma migrate deploy` + dormant `Procfile release:`; also fixed an admin login-route shadow so the seeded superadmin can actually log in. Proven by `verify-seed-safety.mjs` (fresh DB → migrate+seed → superadmin login 200, idempotent, prod-safe); re-proven end-to-end by `verify-e2e-fullpath.mjs` (empty → migrate → core seed → superadmin full MFA login on one DB, S7). **Full deploy wiring (platform runs `db:migrate`) → Wave 8.** |
| R-22 | Admin Agencies page sends `Bearer null` (wrong token key) | Whole agency-verification admin screen non-functional | ✅ **Wave 3 / Session 7 (2026-08-04)** — the Agencies page read `localStorage.getItem('adminToken')` (a key nothing sets → `Bearer null` → every approve/reject/list 401'd); now reads `sessionToken` (what admin verify-mfa stores, like every other admin page). Proven by `verify-wave3-e2e.mjs` [J1]: superadmin approves a pending agency (200, `data.status:'APPROVED'`); approve with no token → 401. |
| R-23 | `mockAssessment.routes` + dummy-admin middleware reachable in prod | Integrity bypass / fake scoring injection | Wave 1 |
| R-44 | Public signup accepts `role:'admin'` (`SignupSchema` enum) → anyone mints an admin-role session token that `requireCompanyMember` treats as cross-tenant god-mode | Privilege escalation / cross-company data access via self-registration | ✅ **Wave 1 / Session 7** — `'admin'` removed from the signup role enum; proven by `verify-authz-matrix.mjs` (signup `{role:'admin'}` → 400). Discovered during the Session-7 authz-matrix build. |

## 🟡 Medium

| ID | Risk | Impact | Fixed in |
|---|---|---|---|
| R-24 | `/resume/extract` dangling call | Resume autofill 404s | ✅ **Wave 3 / Session 7 (2026-08-04)** — the dangling caller was **dead code** (zero call-sites); the real resume→autofill runs **in-process** via `extractSkillsFromText` (`lib/openai.ts`) → `documentProcessor.service` → `autofill.service`, not an ai-service HTTP hop. Removed the dead helper + its stale "CHANGE THIS PATH" placeholder from `profile.service.ts`. Proven by `verify-wave3-e2e.mjs` [J2]: the autofill-preview route is mounted + auth-guarded (a real endpoint, not the dangling dead-end) + a static assert that the dead call is gone. |
| R-25 | Session-management router never mounted | "Sign out other devices" broken | ✅ **Wave 3 / Session 4 (2026-08-03)** — `app.ts` mounts the real `sessionRoutes` at `/api/v1/auth/sessions` (was re-mounting `authRoutes`, so every session call 404'd). Frontend already complete. Proven live by `verify-session-realtime.mjs`: 2 sessions → terminate-others → revoked token 401, current 200 (static + runtime teeth, fail-proved). |
| R-26 | Doc-validation webhook wrong port (4000 vs 5000) | Deep-validation results silently lost | ✅ **Wave 3 / Session 7 (2026-08-04)** — the callback URL is built from `getBackendUrl()` (`:5000` default, centralised in `config/appUrls.ts` during Session 6) and the receiver is mounted at `/api/v1/webhooks/document-validation` with `internalAuth`, before the jobRoutes auth-wall. Proven live by `verify-wave3-e2e.mjs` [J5]: webhook no-header → 401, wrong internal token → 403, correct token → 200 (results reach the backend). |
| R-27 | `NEXT_PUBLIC_API_URL` vs `NEXT_PUBLIC_BASE_URL` conflict | Class of frontend calls 404 depending on env | ✅ **Wave 3 / Session 6 (2026-08-04)** — collapsed to one convention (`NEXT_PUBLIC_API_URL` = bare host) + one resolver `frontend/src/lib/config/api.ts`; ~72 files / ~138 sites repointed. Proven by `verify-api-config.mjs` (grep-gate + FE resolver probe, both fail-proved) and re-exercised end-to-end by `verify-wave3-e2e.mjs`. |
| R-28 | No security headers (helmet wrong framework); rate-limit only on auth | XSS/clickjacking exposure; brute-force/DoS | Wave 1 |
| R-29 | 7-day tokens, no rotation; `session_id` defaults to `'bypass'`; optional blacklist | Weak session/logout enforcement | Wave 1 |
| R-30 | Missing indexes on hot tables; unbounded firehose tables (UsageAnalytics, telemetry, logs) | Slow queries, DB bloat as data grows | ✅ **Wave 2 / Session 4 + 5 + 7** — 36 curated hot-table indexes (S4; `verify-index-coverage.mjs`: 5 hot queries `EXPLAIN` Index-Scan + Seq-Scan control at 20k rows) + opt-in age-cutoff retention reaper for 6 firehose tables (S5; `verify-pagination.mjs` retention mutation) + read-side pagination caps (S5). Re-proven end-to-end at the gate (S7; `verify-e2e-fullpath.mjs` — Index-Scan @20k on the composed-journey DB). **Hostile-review hardening (2026-07-29)**: closed the remaining unbounded reads flagged as "still in-memory" — `getClientsForAgency` now pages distinct clients DB-side; the three agency `*DashboardStats` functions rewritten to DB `count`/`_sum`/`groupBy` and **proven equal to the old in-memory logic** by `verify-agency-dashboard-stats.mjs` (golden fixture); deep-offset OFFSET-DoS closed (`MAX_PAGE` 100000→1000). **Remaining Category-B:** `getAnalyticsCases`/`computeAnalytics` (month-over-month, avg processing time) still in-memory — deferred (needs its own golden-data session). Native/pg_partman partitioning → **Wave 8**. |
| R-31 | No structured logging / correlation IDs / metrics endpoint / APM | Outages are invisible; no debugging trail | Wave 7 |
| R-32 | `sendEmail` swallows errors | Users stuck unverified with no signal | ✅ **Wave 3 / Phase 3.4 (Session 1, 2026-08-01)** — `email.util.ts` now returns an observable `{delivered,messageId?,error?}` and logs failures at error level instead of swallowing to `void`; `sendVerificationEmail` bubbles the result; `signup` observes every send and returns additive `emailDelivered` (stays 201 — email best-effort, but no longer silent). Proven by `verify-error-envelope.mjs` (broken SMTP → signup `data.emailDelivered:false`). Delivery-reliability/queue+retry infra → still Wave 7. |
| R-33 | Mongo hard-required at boot but stores no data | Unused dependency can crash-loop prod | ✅ **Wave 2 / Session 6** — **removed entirely** (chosen over keep-but-non-fatal: 0 data, 0 reads). Deleted `lib/mongo.ts` + `server.ts` wiring, dropped `mongodb`/`@types/mongodb`, purged `MONGO_URI`/`DEBUG_MONGO`/`FORCE_SKIP_MONGO` from `.env.example`+matrix and the dead live Atlas cred from `backend/.env`. Backend now boots with no Mongo env (`/health` db=up), proven by `verify-data-model.mjs`; re-exercised by `verify-e2e-fullpath.mjs` (real boot to `/health` db:up in the composed journey, S7). |
| R-34 | Prompt-injection surface (scraped/OCR content → Gemini) | Skewed verdicts/scores driving hiring | Wave 4/6 |
| R-35 | `postal_code Int`; certifications triplicated; 44/142 relations missing `onDelete` | Data-loss/insert failures; inconsistent deletes | ✅ **Wave 2 / Session 6** — `postal_code`→`String` (alphanumeric codes); certifications de-duplicated (`Certification` table = single source of truth, the drifting `CandidateProfile.certifications` JSON mirror dropped, search rewritten to a table `EXISTS`); **all 142 FKs now declare explicit `onDelete`** (matrix: 23 Cascade owned-data fixes + 2 audit SetNull + nullable columns; rest already default-correct). Hard user-delete now succeeds for company_admin/agency/candidate with owned PII cascaded and audit rows de-identified (was FK-blocked). Live-proven by `verify-data-model.mjs`. |
| R-36 | Response-envelope inconsistency; unhandled promise paths | Sporadic `undefined`/crash bugs | 🔶 **Wave 3 / Phase 3.4 (Session 1, 2026-08-01) — backbone done, migration in progress.** Central `errorHandler` + `notFoundHandler` wired as terminal middleware in `app.ts`; typed `AppError` classes (`errors/httpErrors.ts`) + one envelope (`utils/apiResponse.ts`, `{success,data}`/`{success,error}`). Unhandled async rejections now reach the handler (Express-5 auto-forward, proven) → generic 500, **no stack/hang, no prod leak**. Auth slice fully migrated to the envelope + typed errors (smoke consumer). Proven by new `verify-error-envelope.mjs` (+ fail-proved teeth). **Remaining:** wrap non-auth controllers (candidate/company/agency/session-mgmt/OAuth) + frontend consumers → per feature-slice S2–S6 (Phase 3.6). **Wave 3 / Session 7 (2026-08-04) — canonical-journey slice closed:** `admin.agency.controller` normalized to `sendSuccess` + typed errors; the confirmed frontend envelope mis-reads on the *already-normalized* auth endpoints fixed (the silent-logout `refresh.ts` bug + the login/signup/2FA/onboarding/google-callback reader cluster) + shared `extractApiError` reader; the canonical journeys proven no-silent-dead-end by `verify-wave3-e2e.mjs` (refresh-cycle, admin approve, sign-out, chat, autofill-guard, deep-validation webhook — fail-proved on the refresh shape). **Still carried forward:** the ~53 non-journey controllers + their frontend readers (search/OCR/invites, admin company-verification `{ok}` client, session/message controllers that already emit `{success,data}`) → later slices. |
| R-37 | Redundant subsystems (Question/QuestionBank, 4 assessments, 2 scoring, 2 chat) | Split query paths, maintenance hazard, confusion | Wave 4 · **Wave 2 / Session 6** — dropped the confirmed-dead `question_bank`, `chat_history`, and write-only `candidate_global_score`(+history) tables + writer code (`verify-data-model.mjs` asserts gone). · **Wave 4 / Session 4 (Consolidation A — Questions + Chat, 2026-08-08)** — converged Questions onto canonical `Question`/`QuestionService`→`prisma.question`: deleted the dead `QuestionGenerator.service.ts` + fully-commented `mockQuestionService.ts` + their controller wiring (live generator = `aiQuestionGenerationService`); deleted both orphaned chat `message/mockData.ts` stores (shells already on the real message API). Locked by new `verify-subsystem-consolidation.mjs` (static + compile-fail assert + LIVE canonical probe) + `verify-session-realtime.mjs` (real thread still renders post-delete). · **Wave 4 / Session 5 (Consolidation B — Assessments + Scoring, 2026-08-09)** — the "4 assessments" were actually 3 distinct LIVE flows (employer-invited/self-directed-AI/simple-test) + an already-deleted `mockAssessment` chain, so features were **kept**, not merged; instead consolidated the real duplication: the **three** per-question scorers (`assessmentScoring`+`simpleTest`+`skillRadarFromAssessment`) now delegate to one pure `utils/assessment-scoring-core.ts` (Flow B scores byte-identical; radar/#2 edge-cases reconciled to the authoritative scorer = latent-bug fix). Removed dead compete-simulate + mock/spoofable candidate surfaces (`/candidate/question`,`/candidate/results`,`/code-run`, Flow A instructions page + 60s fake countdown); fixed the employer→candidate link (`inviteToAssessment` now requires ACTIVE). Profile scoring left separate (different domain). Locked by the extended `verify-subsystem-consolidation.mjs` (static + pure-core unit test + parity/determinism probe + invite-gate probe). **R-37 essentially closed** (residual: flagged dead compete-leaderboard subsystem). |

## ⚪ Low / hygiene

| ID | Risk | Impact | Fixed in |
|---|---|---|---|
| R-38 | Dual lockfiles (npm + pnpm) at 3 levels | Non-reproducible installs | Wave 0 |
| R-39 | Duplicate/wrong deps (bcrypt+bcryptjs, redis+ioredis, `crypto`, `@fastify/*`, 2 Pinecone clients, zod v3/v4) | Bloat, inconsistency | Wave 0 |
| R-40 | Dead files/entry points, committed backups/logs/temp/`dev.db`, `.lnk` | Repo bloat, confusion, accidental use | Wave 0 |
| R-41 | UTF-16 `requirements.txt`; unpinned Python deps; google-generativeai/pydantic version spread | Docker build breakage, drift | Wave 0/8 |
| R-42 | `generateBuildId: Date.now()`; `cache:"no-store"` everywhere; no `staleTime` (frontend nav perf) | Slow UX, busted browser caching | Wave 6 |
| R-43 | Corrupt `[object Promise]` line in `backend/.env` | Confusing config errors | ✅ **Wave 0 / Session 5** — line removed; guarded by `verify-config-hygiene.mjs` |

---

## How to use this register

- When a wave starts, its file lists the R-IDs it must close.
- When a risk is fixed, mark it in the wave's checklist and add a [`PROGRESS-LOG.md`](PROGRESS-LOG.md) entry referencing the R-ID.
- A risk is only "closed" once its fix is **verified** (test, manual check, or reproduction gone) — not merely coded.
